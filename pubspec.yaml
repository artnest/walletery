name: walletery
description: Wallet application with encryption and password management functionality

version: 1.0.0+1

environment:
  sdk: ">=2.2.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2

  moor_flutter: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner:
  moor_generator: ^1.3.0

flutter:
  uses-material-design: true
